with-expecter: True
inpackage: True
dir: "{{ .InterfaceDir }}"
mockname: "Mock{{ .InterfaceName }}"
outpkg: "{{.PackageName}}"
filename: "mock_{{ .InterfaceName | snakecase }}_test.go"
recursive: true
packages:
  github.com/trancong12102/sish-gitlab-keys:
    interfaces:
      Authorizer:
      GitlabAPI:
      AuthHandler:
      HTTPHandler:
